{
  "name": "catcher-monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "oxlint packages",
    "format": "oxfmt packages --write",
    "dev": "pnpm -r dev"
  },
  "devDependencies": {
    "oxfmt": "^0.48.0",
    "oxlint": "^1.65.0",
    "typescript": "6.0.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "msw",
      "sharp",
      "@rolldown/binding",
      "@rolldown/binding-darwin-arm64",
      "@img/sharp-libvips-darwin-arm64",
      "@img/sharp-darwin-arm64"
    ]
  }
}
